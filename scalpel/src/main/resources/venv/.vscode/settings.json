{
  "terminal.integrated.env.linux": {
    "PYTHONPATH": "${env:HOME}/.scalpel/.extracted/python:${env:PYTHONPATH}",
    "PATH": "${env:HOME}/.scalpel/.extracted/python:${env:PATH}"
  },

  "python.analysis.extraPaths": [
    "${env:HOME}/.scalpel/extracted/python"
  ]
}
